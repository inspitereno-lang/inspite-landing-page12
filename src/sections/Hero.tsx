import { BackgroundPaths } from "@/components/ui/background-paths";

const Hero = () => {
  return (
    <section id="hero" className="w-full relative">
      <BackgroundPaths title="Infynix Solutions" />
    </section>
  );
};

export default Hero;
